#include <stdio.h>

int main() {
    int tableau[7];
    int *pointeur = tableau;
    int somme = 0;

    // Remplissage du tableau
    printf("Entrez 7 entiers :\n");
    for (int i = 0; i < 7; i++) {
        printf("Entier %d : ", i + 1);
        scanf("%d", pointeur
::contentReference[oaicite:0]{index=0}

